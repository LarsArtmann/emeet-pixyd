# emeet-pixyd — TODO List

**Updated:** 2026-09-19 (harvest + fix-on-sight sweep after the preset-pull session: pull's all-slots error now carries the failure count; docs freshness fixed — README/website preset pages, map doc GETs row, lock-comment; new rows #167–#172 routed from `docs/status/2026-09-19_12-05` §f; preset-pull coverage measured at 94–100% on every pull path, vmTest green this session)

**Previous:** 2026-09-19 (preset-pull session: `preset pull` implemented + web surface; Beta.25 x64 send/parse decodes landed — version-shift retracted, #152 closed with the absence finding, echo routing mask + preset response shapes implemented; remaining rows are hardware/session-external)

> Completed work lives in `CHANGELOG.md` — it does NOT live here. Long-term ideas, design-heavy items, "decided won't-do" decisions, and open questions live in `ROADMAP.md`. This file is **open work only**.

---

## Status Legend

- 🔶 PARTIAL — Started but incomplete
- 🚫 BLOCKED — Waiting on an external unblock (permission, decision, upstream, hardware)

---

## Blocked (highest impact — needs an external unblock)

| #   | Status     | Task                                                                                                                                                                                                                                                                                                                                                                                                     | Impact   | Effort | Evidence                                                                                                      |
| --- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | ------------------------------------------------------------------------------------------------------------- |
| 129 | 🚫 BLOCKED | **Retake web UI screenshots with the PIXY connected** (online state: live MJPEG preview, tracking active) — current shipped screenshots show the offline UI. Regenerate `webui-panel.png` crop + video poster from the online shots. Folds into the #166 hardware session                                                                                                                                | MED      | S      | `2026-08-17_18-53` §a.4/§e.3 — camera was disconnected during capture; files in `website/public/screenshots/` |
| 154 | 🚫 BLOCKED | **Close issue #6** with the closing formula once @zutto confirms the PIXY 2K works on real hardware (follow-up comment `issues/6#issuecomment-5716075020` already posted; release ref v0.4.0 available)                                                                                                                                                                                                  | HIGH     | S      | `2026-09-17_17-34` c1/f2                                                                                      |
| 155 | 🚫 BLOCKED | **Cut v0.4.1** — Lars's cadence call vs accumulating toward v0.5. The release now carries the post-tag fixes (model-aware output, permission warning, vmTest fix) **plus the five new HID command families** (speed, tracking variants, battery, preset push, identity queries) and preset pull + the framing/enum corrections. Release-time gates: local `govulncheck` + fuzz-seed smoke before tagging, module-proxy verification after (`12-05` §f 19/47)                                                                                                                                            | MED      | S      | `CHANGELOG.md` [Unreleased]                                                                                   |
| 156 | 🚫 BLOCKED | **Branch protection requiring the three workflows** (go-test, nix, website) on master — GitHub settings only Lars can change; makes the ungated-auto-commit failure class impossible to merge                                                                                                                                                                                                            | MED      | S      | `2026-09-17_17-34` f16                                                                                        |
| 166 | 🚫 BLOCKED | **M27 hardware-verification bundle** (PIXY attached): one session closes ~6 open threads — run `TestIntegration_BatteryProbe` (battery verdict → #139); pin V2 response framing + `MotorType`/`TargetTrackMode` enum values + motor iface byte (→ #150); pin speed unit + hardware limit (→ #138); preset slot-count sweep + which preset-response shape the firmware answers — mode-only GET vs full SET-echo (→ #141); hardware-verify tracking variants; retake online screenshots (#129); live `preset push` → `preset pull` round-trip check (values must match — `12-05` §f 12); optional Windows usbmon capture to settle `MotorType` from real caller traffic (Lars's scope call, `12-05` §g Q3) | **HIGH** | M      | `2026-09-19_06-44` §b/§f 1–7; `TestIntegration_BatteryProbe` in `integration_hardware_test.go`                |

---

## TODO

| #   | Status     | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Impact  | Effort | Evidence                                                            |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ------ | ------------------------------------------------------------------- |
| 138 | 🔶 PARTIAL | **Finish PTZ speed** — implemented AND wired (`speed` persists to `state.json` via `pixy.SpeedValues` omitzero; re-asserted before every move: PTZ moves, preset load/push, center, reconcile-on-appear; web sliders initialize from persisted state; `v4l2Mu → hidMu` lock order pinned by `TestLockOrder_V4L2MoveWithHIDCommands`); remaining: hardware-verify the unit + real limit, then clamp at the command layer and set the slider max (#166)                                                                                                  | MED     | S/M    | `motor.go`, `speed_wiring_test.go`; shipped `81955d5`               |
| 139 | 🔶 PARTIAL | **Finish battery/charge surface** — implemented (`battery` command + status/Waybar/web lines, 60s TTL cache per ADR `2026-09-19_battery-polling-on-demand-ttl.md`, graceful absence; ChargeSta semantics wired: typed `pixy.ChargeStatus`, `{1,2}`=charging from the Beta.25 consumer-code decode, Waybar `charging`/`discharging` classes pinned by `TestWaybarBatteryClass`); remaining: hardware verdict — the wired PIXY answering battery queries is UNVERIFIED (#166)                                                                            | MED     | S/M    | `power.go`, `power_test.go`; `tools/emhid/cmdtable.json`            |
| 140 | 🔶 PARTIAL | **Finish tracking variants** — implemented AND persisted (`tracking <none\|face\|halfbody\|fullbody>` persists to `state.json`, web picker shows the truth after restart, pinned by `TestTrackingVariant_PersistedAcrossRestart`); enum corrected to the official 1-based scheme (0=none, 1=face, 2=halfbody, 3=fullbody — Beta.25 UI-registration evidence, `TestTargetTrackModeWireValues` pins the bytes; numeric CLI aliases removed); remaining: hardware confirmation (#166) + whether reconcile should re-assert the variant after power cycles | MED     | S      | `internal/pixy/v2head.go`, `speed_wiring_test.go`                   |
| 141 | 🔶 PARTIAL | **Finish preset push/pull** — implemented both directions (`preset push <name>`: SetMotorPos ×3 + SetMotorPresetPos, alphabetical slot mapping capped at assumed 8 slots; web push button with confirm() prompt at `POST /api/preset/push/{name}`; `preset pull`: read-only sweep of slots 1..8 via `GetMotorPresetPosMode`, storing full-shape answers as additive `hw-<slot>` presets and counting mode-only answers as "set (position not exposed)" — the Beta.25 GET parser is single-byte, positions ride the SET echo, both shapes parsed and #166 pins which the wired firmware answers; never overwrites existing names; web Pull button at `POST /api/preset/pull`); remaining: slot-count sweep + response-shape pin on hardware (#166); if the wired firmware answers mode-only, position acquisition via the SET echo needs Lars's product call (it mutates slot mode — `12-05` §g Q1)  | LOW/MED | S | shipped `db4943e`; `commands.go`, `motor.go`; ROADMAP pull design implemented + evidence-corrected 2026-09-19 |
| 148 | 🔶 PARTIAL | **innoextract upstream PR** — `tools/inno661/UPSTREAM.md` staged (issue text with the characterized 6.6.1 deltas, PR outline against `setup/`, pre-send checklist); remaining: sending is Lars's call                                                                                                                                                                                                                                                                                                                                                  | MED     | S      | `tools/inno661/UPSTREAM.md`                                         |
| 150 | 🔶 PARTIAL | **Decode remaining enum values + response framing** — statically decoded from the Beta.25 x64 disassembly: response framing (head-echo + reserved dword + payload@8, min 9 — CORRECTED in our parsers, `pixy.V2ResponsePayloadOffset`), echo routing mask (`resp[1] & 0x1F`, implemented in `pixy.v2EchoMatches`), `ChargeSta` ({1,2}=charging), `TargetTrackMode` (1-based + none — CORRECTED), battery level byte, per-command min lengths for the preset/default-pos/speed families (§3.5a); `MotorType` + DefaultPosMode value semantics still assumed — the MotorType send site passes the byte through from vtable-indirect callers, so the value decode needs hardware tracing; all corrections are hardware-unconfirmed (#166 confirms + decodes response bytes 4..7)   | HIGH | M | `internal/pixy/v2head.go`; `docs/hid-protocol-official-map.md` §3.5 |
| 167 | 🔶 PARTIAL | **Pull early-abort heuristic** — stop the `preset pull` sweep after N consecutive slot timeouts instead of always paying 8 × 500 ms (~4 s worst case on a half-dead device); pick N and surface "aborted after N dead slots" in the summary (`12-05` §e.8/§f 20)                                                                                                                                                                                                                            | MED     | S      | `commands.go` `handlePresetPull`; report `12-05` §e.8          |
| 168 | 🔶 PARTIAL | **Simulator fidelity knobs** — wrap `presetFullResponses` (currently a raw struct field poked by tests) into a builder option before more knobs accumulate, and model the speed-query duality (SET-head vs `09 03 01 13`) so #166 comparisons can exercise both forms (`12-05` §e.9/§f 32/35)                                                                                                                                                                                              | LOW     | S      | `pixy_simulator_test.go`, `pixy_simulator_v2_test.go`          |
| 169 | 🔶 PARTIAL | **Property test: pull is additive** — randomized name-collision inputs prove `preset pull` never evicts or mutates existing presets and never exceeds the 16-slot cap (`12-05` §f 34)                                                                                                                                                                                                                                                                                                      | LOW     | S      | `preset_pull_test.go`; `pixy.PresetMap`                        |
| 170 | 🔶 PARTIAL | **Decide dprint ownership** — `dprint.json` is configured but the binary is absent from the devShell, so markdown formatting has no local runner and TODO/ROADMAP tables are hand-aligned: vendor dprint into the devShell OR delete the config (`12-05` §e.11/§f 37)                                                                                                                                                                                                                       | LOW     | S      | `dprint.json`, `flake.nix` devShell                            |
| 171 | 🔶 PARTIAL | **`preset pull --dry-run`** — report-only sweep (same slot queries, no state writes) as the safe discovery surface; pairs naturally with #167 (`12-05` §f 46)                                                                                                                                                                                                                                                                                                                             | LOW     | S      | `commands.go` `handlePresetPull`                               |
| 172 | 🔶 PARTIAL | **Extract a shared V2 query helper** — `identity.go`/`power.go`/`motor.go` each wrap the same hidMu + `v2Read` + echo-validate pattern; extract one helper when the next GET family lands (4th caller is the trigger) (`12-05` §f 48)                                                                                                                                                                                                                                                     | LOW     | M      | `identity.go`, `power.go`, `motor.go`                          |

---

All completed work lives in `CHANGELOG.md` ([Unreleased] carries the 2026-09-17 → 09-19 work). Design decisions and open questions live in `ROADMAP.md` — #116 (structured command types) and #123 (multi-word preset names) now have recommendation ADRs (`docs/adr/2026-09-18_structured-command-types.md`, `docs/adr/2026-09-18_multi-word-preset-names.md`) awaiting Lars's decision.

> Full ranked backlog with rationale: `docs/status/2026-09-19_06-44_pareto-plan-execution-full-session.md` §f — items not covered above are ROADMAP-grade research or micro-hygiene.
