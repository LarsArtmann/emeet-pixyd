# Status Report — 2026-09-17 13:46

**Session scope:** Fix GitHub issue #6 ("toggle/config option for the /proc monitor" + EMEET PIXY 2K uses product ID `0x0118`), including one forced self-review round and one full re-audit round. This report covers **only this session's work and what was noticed in passing** — no new research beyond it.

**TL;DR:** Issue #6 is functionally fixed and verified locally (code, tests, docs, CHANGELOG, website build, issue reply posted). Master's CI lint breakage (pre-existing, 9 findings) was repaired. Three gaps from the first "done" claim were caught in re-audit and closed. Nothing is pushed or deployed — origin/master is still red until Lars pushes. Issue #6 stays open pending the reporter's confirmation on real 2K hardware.

**Resolution:** mostly executed by the follow-up sessions — ~~nothing pushed/deployed~~ pushed + v0.4.0 tagged + site deployed (`02f769c` era, `a12` in `2026-09-17_17-34`); #137 decided+implemented (`140bc98`); startup sync shipped (part of reconcile); pre-commit gate shipped (`14fb43b`); exhaustruct_v5 done; model logging/output done (`9869a39`). STILL OPEN: #6 closure (awaits @zutto), online screenshots (TODO #129, folds into the #166 hardware session). ~~vmTest (TODO #157), `0118` fuzz home (TODO #165)~~ both landed 2026-09-19.

---

## a) FULLY DONE

| #  | What                                                                                                                                                                                                                                                                                                                                                                                       | Evidence                                                                                                                                                                                                                                                                        | Scope                                                                                                   |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 1  | **PIXY 2K (`328f:0118`) probe support** — `isPixyProductID()` helper; `matchesPixyID` accepts both `00c0` (original) and `0118` (2K) for both video4linux (`PRODUCT=`) and hidraw (`HID_ID=`) uevent formats                                                                                                                                                                               | New tests `TestProbeVideo4linux_PIXY2KFound`, `TestProbeHidraw_PIXY2KFound` (uses the literal hardware-reported `HID_NAME=EMEET EMEET PIXY 2K`), 2 new `TestHasPixyProduct` cases; full suite green with `-race -count=1`; committed by daemon in `0288e65`/`cc6af44`/`18201df` | `probe.go`, `probe_video_test.go`, `probe_hidraw_test.go`                                               |
| 2  | **udev rules accept both product IDs** — `ATTRS{idProduct}=="00c0                                                                                                                                                                                                                                                                                                                          | 0118"`in the NixOS module (hidraw + video4linux rules).`\|`alternation verified against local`udev(7)` man page (supported since systemd 217), not just assumed                                                                                                                 | `modules/nixos.nix` edited; `nix flake check` passes (module evaluates)                                 |
| 3  | **`/proc` monitor toggle guaranteed by test** — `TestAutoManage_AutoOff_NoAction` now injects a failing `isCameraInUse` and asserts it is **never invoked** with `auto=off`; camera state untouched                                                                                                                                                                                        | Test green under `-race`; this pins the exact behavior zutto asked for in #6                                                                                                                                                                                                    | `auto_manage_test.go`                                                                                   |
| 4  | **NixOS `auto` option description clarified** — `off` now reads "manual control: no /proc monitoring, camera keeps whatever mode you set"                                                                                                                                                                                                                                                  | `modules/nixos.nix`; flake check green                                                                                                                                                                                                                                          | `modules/nixos.nix`                                                                                     |
| 5  | **Manual-mode documentation** — new "Manual Control" section in README (with correct `#manual-control` anchor, verified) + rewritten "Manual Mode (`off`)" section in the website auto-modes guide, framed for the headless/Home Assistant snapshot use case; env-var table and troubleshooting rows updated with both USB IDs                                                             | README edited; website builds: **19 pages, CSP hash patch 19/19**                                                                                                                                                                                                               | `README.md`, 5 website MDX files                                                                        |
| 6  | **Fixed 9 pre-existing master CI lint failures + 1 own** — `ratelimit.go` (`exhaustruct`, `gochecknoglobals`, `unparam`), `probe_warn_ratelimit_test.go` (`paralleltest`, 3× `wsl_v5`), `ratelimit_test.go` (2× `wsl_v5`), plus my own `dupword` on the literal 2K HID name. Fixes: targeted `//nolint` (repo convention), whitespace, genuinely varied `newWarnLimiter` interval in tests | `golangci-lint run --timeout 2m` → **0 issues** (was 10); CI job `Lint` log confirmed the same findings pre-fix                                                                                                                                                                 | `ratelimit.go`, `ratelimit_test.go`, `probe.go`, `probe_warn_ratelimit_test.go`, `probe_hidraw_test.go` |
| 7  | **CHANGELOG entries** — 2K support + manual-mode docs under Unreleased/Added; master CI lint fix under Unreleased/Fixed                                                                                                                                                                                                                                                                    | `CHANGELOG.md`                                                                                                                                                                                                                                                                  | `CHANGELOG.md`                                                                                          |
| 8  | **Docs inventory updated** — `AGENTS.md` (header + probing bullet), `FEATURES.md` (probing row) now mention both product IDs and `isPixyProductID()`                                                                                                                                                                                                                                       | edited                                                                                                                                                                                                                                                                          | `AGENTS.md`, `FEATURES.md`                                                                              |
| 9  | **TODO_LIST #137 filed** — device-reappear semantics (re-assert vs adopt), with evidence pointer to `main.go` eventLoop; `Updated:` header bumped to 2026-09-17                                                                                                                                                                                                                            | `TODO_LIST.md`                                                                                                                                                                                                                                                                  | `TODO_LIST.md`                                                                                          |
| 10 | **Issue #6 reply posted** as Lars — voice-checked via github-voice (`check-draft.py`: 0 FAIL, 0 WARN), explains both fixes + the ffmpeg-snapshot→privacy caveat                                                                                                                                                                                                                            | [issuecomment-5713676151](https://github.com/LarsArtmann/emeet-pixyd/issues/6#issuecomment-5713676151)                                                                                                                                                                          | GitHub                                                                                                  |
| 11 | **Full verification gate** — `go vet`, `go test -race -count=1 ./...`, `golangci-lint` (0 issues), `nix flake check`, website build: all green; working tree clean (daemon committed everything)                                                                                                                                                                                           | Final gate exit 0 at 13:45                                                                                                                                                                                                                                                      | repo-wide                                                                                               |

## b) PARTIALLY DONE

| Item                              | What works now                                                                                                                                                                   | What remains open                                                                                                                                     | Blocker                                                                         | Effort                       |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ---------------------------- |
| **Issue #6 closure**              | Code + tests + docs + CHANGELOG + posted reply                                                                                                                                   | Issue still OPEN; no confirmation from @zutto that it works on his real 2K device                                                                     | Lars owns only the original `00c0` — cannot verify `0x0118` locally             | S (close after confirmation) |
| **Getting the fix to users**      | Everything committed on local master                                                                                                                                             | **Nothing pushed** — origin/master is behind and its CI `Lint` job is currently RED (run `35213951519`, same 9 findings I fixed locally)              | Harness rule: never push without Lars's explicit instruction                    | S (one `git push`)           |
| **Website docs deployment**       | MDX updated + local build verified (19 pages)                                                                                                                                    | `emeet-pixyd.lars.software` still serves the old docs — 2K + Manual Mode not live                                                                     | Deploy is manual (`firebase deploy`, per AGENTS.md); TODO #133 (CI/CD) not done | S                            |
| **2K support verification depth** | Unit tests cover all uevent ID formats (compact/leading-zeros/uppercase for 00c0; compact + leading-zeros + hidraw 8-digit for 0118); udev alternation verified against man page | No end-to-end test on physical 2K hardware (HID byte layout, PTZ ranges, 200ms timing all _assumed_ identical per reporter's "worked out of the box") | No 2K device available                                                          | M (needs hardware)           |

## c) NOT STARTED

| Item                                                                                                                                                                                                            | Why not started                                                              | Priority                                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | --------------------------------------------------------------- |
| **TODO #137 — device-reappear semantics** (re-assert persisted `privacy`/`tracking` vs adopt hardware state after reboot/replug)                                                                                | Needs Lars's design decision; deliberately not snuck in as a behavior change | High (weakens privacy guarantee + the persistence I documented) |
| **Startup sync** — daemon never `syncState`s at boot when the device is already present; fresh install believes `privacy` while hardware is actually idle/lens-open (subset of #137, but independently fixable) | Same design question as #137                                                 | High                                                            |
| **Docs nuance: power-cycle caveat** — README/website say modes "persist until you change it"; true while powered, but after reboot/replug the daemon adopts the hardware default                                | Noticed during re-audit; small honest-docs fix, not yet written              | Medium                                                          |
| **Website CI** building the MDX (would have caught MDX breakage without manual build)                                                                                                                           | Out of session scope; related standing TODO #133                             | Medium                                                          |
| **Push + release cut** — 2K support is user-facing; a tag/release would make it reachable                                                                                                                       | Push requires Lars's go (see questions)                                      | High                                                            |
| Standing backlog untouched this session: TODO #124, #129–#136 (all pre-existing, none blocked by this session)                                                                                                  | Deprioritized vs issue #6                                                    | as filed                                                        |

## d) TOTALLY FUCKED UP!

1. **Master CI was RED when I arrived — and nobody had noticed.** An auto-committed `ratelimit.go` (the warn-rate-limiter from an earlier session) shipped with **9 golangci-lint failures**; the `Lint` job on the `push master` run (`35213951519`) failed, and two dependabot PR runs failed the same way. Severity: blocks every merge/PR signal. Root cause: **the auto-commit daemon commits without running lint gates** — this is at least the second time heuristic commits damaged the repo (cf. go-paperless lesson in global AGENTS.md). Mitigation: fixed locally (0 issues); origin stays red until push. **The process hole itself is NOT fixed.**
2. **My first "done" claim was premature.** I posted the public issue comment _before_ writing CHANGELOG entries and _before_ verifying the website build — it took Lars's "Is that all?!?!" challenge to catch the gaps (CHANGELOG missing, website build unverified, #137 design gap untracked). Root cause: treated "tests + lint green" as the finish line instead of the full doc/deploy artifact chain. Mitigation: all three closed in round 2; lesson recorded in (e).
3. **Entire session ran against a broken LSP.** `go.mod` requires go ≥ 1.26.7, gopls/golangci-lint-LS run 1.26.5 with `GOTOOLCHAIN=local` → every diagnostic was `context loading failed` noise; all verification had to go through `nix develop`. Pre-existing environment issue, NOT fixed this session — it will degrade the next session too.

## e) WHAT WE SHOULD IMPROVE

1. **Gate the auto-commit daemon on lint** (or add a pre-commit hook running `nix develop -c golangci-lint run ./...`). The daemon silently pushed master red; if it happens again, the same 30-minute cleanup repeats. Impact: high — it can invalidate CI as a signal repo-wide.
2. **Fix the LSP toolchain mismatch** (pin gopls to 1.26.7 or set `GOTOOLCHAIN=auto` in the LSP env). Impact: every Crush session in this repo pays a diagnostic-noise tax and must shell out to nix for truth.
3. **Order of operations for public communication:** code → tests → docs → CHANGELOG → website build → **then** post the GitHub comment. I did it backwards once; the voice checker validates tone, not truth-timing.
4. **Docs-claim vs runtime-truth check:** I documented "modes persist until you change it" while the hotplug path (`main.go:284` → `syncState`) silently adopts hardware state on re-attach. Rule: trace the runtime path for every guarantee before writing it into README.
5. **Migrate `exhaustruct` → `exhaustruct_v5`** in `.golangci.yml` — the linter is deprecated since v2.13 and will disappear; migrate before it turns into a surprise red CI (same failure class as (d)1).
6. **Auto-mode copy is triplicated** (README table + env table, website auto-modes page, NixOS option description) — every wording change touches 3+ places; this session had to do exactly that. Consider single-sourcing or cross-linking (same drift class as TODO #135).
7. **golangci-lint cache warnings** (`/mnt/buildcache` fact-persist failures during runs) — harmless but noisy; investigate cache GC/race.

## f) Next tasks (ranked; harvest candidates for TODO_LIST/ROADMAP)

| #  | Task                                                                                                                           | Impact   | Effort | Category       |
| -- | ------------------------------------------------------------------------------------------------------------------------------ | -------- | ------ | -------------- |
| 1  | Push local master → origin (turns red CI green; ships 2K fix) — **needs Lars's explicit go**                                   | Critical | S      | Release        |
| 2  | Cut a release/tag so 2K users get a reachable version (2K support is user-facing)                                              | High     | S/M    | Release        |
| 3  | Await @zutto confirmation on real 2K hardware, then close #6 with closing formula                                              | High     | S      | GitHub         |
| 4  | TODO #137: decide + implement device-reappear semantics (re-assert persisted mode vs adopt)                                    | High     | S/M    | Feature/Design |
| 5  | Startup `syncState` when device already present (fresh-install belief ≠ hardware)                                              | High     | S      | Bug            |
| 6  | Document the power-cycle caveat next to the Manual Control claims (until #137 lands)                                           | Medium   | S      | Documentation  |
| 7  | Deploy website to Firebase (2K + Manual Mode docs live)                                                                        | Medium   | S      | Deployment     |
| 8  | Gate auto-commit daemon / add pre-commit lint hook (fixes (d)1 class permanently)                                              | High     | M      | Quality        |
| 9  | Fix LSP toolchain pinning (1.26.5 vs go.mod 1.26.7)                                                                            | Medium   | S      | Tooling        |
| 10 | Website CI job that builds the MDX (catches breakage pre-deploy; folds into TODO #133)                                         | Medium   | M      | Quality        |
| 11 | Migrate `exhaustruct` → `exhaustruct_v5` in `.golangci.yml`                                                                    | Low      | S      | Cleanup        |
| 12 | Integration test: manual `track` with `auto=off` survives multiple `autoManage` ticks + daemon restart (state.json round-trip) | Medium   | S      | Testing        |
| 13 | NixOS module test (runVMTest) with fake sysfs asserting both udev product IDs present                                          | Low      | M      | Testing        |
| 14 | Log which PIXY model was found ("PIXY" vs "PIXY 2K") in `probeDevices()` — cheap support-debuggability win                     | Low      | S      | Feature        |
| 15 | Add `0118` uevent samples to fuzz seed corpus (`FuzzReadSignals`/probe paths)                                                  | Low      | S      | Testing        |
| 16 | `device` command output: include detected product/model (helps issue reports)                                                  | Low      | S      | Feature        |
| 17 | Single-source the auto-mode table (README ↔ website ↔ NixOS description) or cross-link                                         | Low      | M      | Cleanup        |
| 18 | Consider `EMEET_PIXYD_PRODUCT_IDS` env override for future variants (ROADMAP fuel — YAGNI until a 3rd model appears)           | Low      | M      | Feature        |
| 19 | `checkExternalDeps`-style startup warning when hidraw/video nodes exist but aren't accessible (permission misconfiguration)    | Low      | M      | Feature        |
| 20 | Uppercase-2K test case for `TestHasPixyProduct` (symmetry with the 00c0 uppercase case)                                        | Low      | S      | Testing        |
| 21 | Investigate golangci-lint buildcache fact-persist warnings                                                                     | Low      | S      | Tooling        |
| 22 | Check `docs/DOMAIN_LANGUAGE.md` (if it exists and names the USB ID, align it)                                                  | Low      | S      | Documentation  |
| 23 | Waybar tooltip: include auto mode for at-a-glance manual/auto state                                                            | Low      | S      | Feature        |
| 24 | Standing: TODO #124 (go-branded-id publish), #129 (online screenshots), #130–#136                                              | as filed | —      | Backlog        |

(24 concrete items this session; padded-to-50 filler intentionally omitted — the standing backlog above carries the rest.)

## g) Questions I cannot answer myself

1. **May I push local master to origin?** Origin/master's CI is red right now and the fix is sitting locally; the harness forbids pushing without your explicit instruction. One word and it's up.
2. **#137 product decision:** when the PIXY re-appears after reboot/replug with hardware state ≠ persisted state, should the daemon **re-assert** the persisted mode (lens re-blocks — stronger privacy guarantee) or keep **adopting** hardware truth (current behavior)? I can argue both from the code; only you can set the product intent.
3. **2K verification provenance:** do you have (or can you borrow) a PIXY 2K to verify end-to-end, or do we rely solely on @zutto confirming before closing #6? This decides whether #6 closes this week or waits on a stranger.

---

_Report written by Crush (glm-5.3) at 2026-09-17 13:46, immediately after the re-audit round. Format: Markdown (`.md`) — explicit user override of the status-report skill's HTML default. The auto-commit daemon will pick up this file; no manual commit per harness contract. Section (f) is pending docs-health HARVEST routing on instruction — deliberately not harvested yet ("THEN WAIT FOR INSTRUCTIONS")._

---

## Resolution (2026-09-18)

Open-item routing as of 2026-09-18: §c "startup sync" → shipped via `reconcileOnDeviceAppear`; §c "power-cycle caveat" → superseded by the persistence-guarantee paragraph (README §Manual Control); §f f18 product-ID env override → ROADMAP; f23 Waybar auto mode → ROADMAP Waybar bullet; everything else → TODO_LIST #154–#165 or CHANGELOG 0.4.0.
